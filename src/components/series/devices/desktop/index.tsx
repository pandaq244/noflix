import * as React from 'react';

import {
    DesktopSeries
} from '../../components/';

import AppWrapper from '../../../default/';

import './style/index.css';

export default () => (
    <AppWrapper class="main-series--desktop">
        <DesktopSeries />
    </AppWrapper>
);
