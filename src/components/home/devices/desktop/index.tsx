import * as React from 'react';

import {
    DesktopHome
} from '../../components/';

import AppWrapper from '../../../default/';

import './style/index.css';

export default () => (
    <AppWrapper class="main-app--desktop">
        <DesktopHome />
    </AppWrapper>
);
