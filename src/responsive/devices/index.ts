import {
    IDevicesProps as IProps
} from '../../interfaces/responsive/';

const devices: IProps = {
    default: {
        minWidth: 1200
    },
    mobile: {
        maxWidth: 720
    }
};

export default devices;
